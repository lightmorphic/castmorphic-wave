name: Deploy website

# Publishes the site/ folder to GitHub Pages (via the gh-pages branch)
# whenever it changes on main.

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - '.github/workflows/pages.yml'

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # The last-updated date has to track real content changes, not the
      # deploy, so it comes from the newest CHANGELOG entry rather than
      # from today's date.
      - name: Stamp the last-updated date from the changelog
        run: |
          DATE=$(grep -m1 -oE '[0-9]{4}-[0-9]{2}-[0-9]{2}' CHANGELOG.md)
          DISPLAY=$(date -u -d "$DATE" '+%-d %B %Y')
          sed -i -E "s|<time datetime=\"[0-9-]+\">[^<]*</time>|<time datetime=\"$DATE\">$DISPLAY</time>|" site/index.html
          sed -i -E "s|(\"dateModified\": \")[0-9-]+|\1$DATE|g" site/index.html
          sed -i -E "s|(<lastmod>)[0-9-]+|\1$DATE|" site/sitemap.xml
          echo "stamped $DATE"

      - name: Push the site to the gh-pages branch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd site
          git init -b gh-pages
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Deploy website"
          git push --force "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" gh-pages
